import { motion } from "framer-motion";
import { useInView } from "react-intersection-observer";
import { LinkedIn, GitHub, Mail, Download } from "lucide-react";
import { Code, Database, Cloud, Globe, Cpu, Terminal } from "lucide-react";
import profileImage from "../assets/profile.jpg";
import { useTheme } from "../hooks/useTheme";

const Hero = () => {
	const { ref, inView } = useInView({ threshold: 0.1, triggerOnce: true });
	const { theme, styles, themeMode } = useTheme();

	const containerVariants = {
		hidden: { opacity: 0 },
		visible: {
			opacity: 1,
			transition: { staggerChildren: 0.2, duration: 0.6 },
		},
	};

	const itemVariants = {
		hidden: { opacity: 0, y: 30 },
		visible: { opacity: 1, y: 0, transition: { duration: 0.6 } },
	};

	const techIcons = [
		{ icon: <Code size={20} />, label: "Frontend Dev" },
		{ icon: <Database size={20} />, label: "Backend Dev" },
		{ icon: <Cloud size={20} />, label: "Cloud Solutions" },
		{ icon: <Globe size={20} />, label: "Full Stack" },
		{ icon: <Cpu size={20} />, label: "System Design" },
		{ icon: <Terminal size={20} />, label: "DevOps" },
	];

	return (
		<Box sx={{ textAlign: "center", color: (theme) => 
  theme.palette.mode === "light" ? "#ffffff" : theme.palette.text.primary 
}}			sx={{
  minHeight: "100vh",
  background: "transparent",
  position: "relative",
  display: "flex",
  alignItems: "center",
  marginTop: { xs: "-70px", md: "-80px" },
  paddingTop: { xs: "70px", md: "80px" },
  "&::before": {
    content: '""',
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    backgroundImage: `url(${profileImage})`,
    backgroundSize: "cover",
    backgroundPosition: "center",
    backgroundRepeat: "no-repeat",
    opacity: 0.15,
    zIndex: 1,
  },
  "&::after": {
    content: '""',
    position: "absolute",
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    background: (theme) =>
      theme.palette.mode === "light"
        ? "rgba(0, 0, 0, 0.7)"        // Dark overlay for white text
        : "rgba(255, 255, 255, 0.1)",  // Light overlay for dark text
    zIndex: 2,
  },
}}>
			<Container maxWidth="lg" sx={{ position: "relative", zIndex: 2 }}>
				<motion.div
					ref={ref}
					variants={containerVariants}
					initial="hidden"
					animate={inView ? "visible" : "hidden"}>
					<Box sx={{ textAlign: "center", color: "white" }}>
						<motion.div variants={itemVariants}>
							<Avatar
								src={profileImage}
								alt="Nermeen Nasim"
								sx={{
									width: 180,
									height: 180,
									margin: "0 auto 2rem",
									border: "4px solid #E17564",
									boxShadow: "0 8px 32px rgba(225, 117, 100, 0.3)",
								}}
							/>
						</motion.div>

						<motion.div
							variants={itemVariants}
							initial={{ x: -100, opacity: 0 }}
							animate={inView ? { x: 0, opacity: 1 } : { x: -100, opacity: 0 }}
							transition={{
								duration: 1.2,
								delay: 0.5,
								type: "spring",
								stiffness: 60,
								damping: 15,
							}}
							whileHover={{
								scale: 1.05,
								transition: { duration: 0.3 },
							}}>
							<Typography
								variant="h1"
								sx={{
									fontSize: { xs: "3rem", sm: "4rem", md: "5rem" },
									fontWeight: 700,
									marginBottom: 2,
									background: "linear-gradient(45deg, #E17564, #BE3144)",
									WebkitBackgroundClip: "text",
									WebkitTextFillColor: "transparent",
									fontFamily: '"Poppins", sans-serif',
									textAlign: "center",
									cursor: "default",
									"&:hover": {
										filter: "drop-shadow(0 0 20px rgba(225, 117, 100, 0.5))",
									},
									transition: "all 0.3s ease",
								}}>
								Nermeen Nasim
							</Typography>
						</motion.div>

						<motion.div variants={itemVariants}>
							<Typography
								variant="h3"
								sx={{
									fontSize: { xs: "1.5rem", sm: "2rem", md: "2.5rem" },
									fontWeight: 600,
									marginBottom: 3,
									color: "#E17564",
									fontFamily: '"Inter", sans-serif',
								}}>
								Software Engineer | Startup Founder
							</Typography>
						</motion.div>

						<motion.div variants={itemVariants}>
							<Typography
								variant="h6"
								sx={{
									maxWidth: "900px",
									margin: "0 auto 3rem",
									lineHeight: 1.8,
									fontSize: { xs: "1rem", md: "1.2rem" },
									color: "rgba(255, 255, 255, 0.9)",
									fontFamily: '"Inter", sans-serif',
								}}>
								Hi, I’m Nermeen Nasim — Founder and Senior Software Engineer at
								Blue Sprout Agency LLC. I’m passionate about transforming ideas
								and existing systems into modern, scalable web solutions. With
								hands-on experience in React, .NET, and REST APIs, I love
								helping businesses grow by simplifying their processes through
								clean, user-friendly design and smart technology. At Blue
								Sprout, I lead projects that bring innovation to small
								businesses — from website design to digital transformation —
								helping them reach new levels of productivity and client
								engagement.
							</Typography>
						</motion.div>

						<motion.div variants={itemVariants}>
							<Box
								sx={{
									display: "flex",
									justifyContent: "center",
									flexWrap: "wrap",
									gap: 1,
									mb: 4,
								}}>
								{techIcons.map((tech) => (
									<motion.div
										key={tech.label}
										whileHover={{ scale: 1.05, y: -2 }}
										whileTap={{ scale: 0.95 }}>
										<Chip
											icon={tech.icon}
											label={tech.label}
											sx={{
												backgroundColor: "rgba(225, 117, 100, 0.2)",
												color: "#E17564",
												border: "1px solid #E17564",
												fontWeight: 600,
												"&:hover": {
													backgroundColor: "rgba(225, 117, 100, 0.3)",
												},
											}}
										/>
									</motion.div>
								))}
							</Box>
						</motion.div>

						<motion.div variants={itemVariants}>
							<Box
								sx={{
									display: "flex",
									justifyContent: "center",
									flexWrap: "wrap",
									gap: 2,
								}}>
								<motion.div
									whileHover={{ scale: 1.05 }}
									whileTap={{ scale: 0.95 }}>
									<Button
										variant="contained"
										size="large"
										startIcon={<LinkedInIcon />}
										href="https://www.linkedin.com/in/n-nasim"
										target="_blank"
										sx={{
											backgroundColor: "#E17564",
											color: "white",
											fontWeight: 600,
											px: 3,
											py: 1.5,
											borderRadius: 3,
											"&:hover": {
												backgroundColor: "#d85246",
												transform: "translateY(-2px)",
												boxShadow: "0 8px 25px rgba(225, 117, 100, 0.4)",
											},
											transition: "all 0.3s ease",
										}}>
										LinkedIn
									</Button>
								</motion.div>

								<motion.div
									whileHover={{ scale: 1.05 }}
									whileTap={{ scale: 0.95 }}>
									<Button
										variant="contained"
										size="large"
										startIcon={<GitHubIcon />}
										href="https://github.com/nermeennasim"
										target="_blank"
										sx={{
											backgroundColor: "#BE3144",
											color: "white",
											fontWeight: 600,
											px: 3,
											py: 1.5,
											borderRadius: 3,
											"&:hover": {
												backgroundColor: "#8b2230",
												transform: "translateY(-2px)",
												boxShadow: "0 8px 25px rgba(190, 49, 68, 0.4)",
											},
											transition: "all 0.3s ease",
										}}>
										GitHub
									</Button>
								</motion.div>

								<motion.div
									whileHover={{ scale: 1.05 }}
									whileTap={{ scale: 0.95 }}>
									<Button
										variant="outlined"
										size="large"
										startIcon={<EmailIcon />}
										href="mailto:nimmi24.1990@gmail.com"
										sx={{
											borderColor: "#E17564",
											color: "#E17564",
											fontWeight: 600,
											px: 3,
											py: 1.5,
											borderRadius: 3,
											borderWidth: 2,
											"&:hover": {
												borderColor: "#E17564",
												backgroundColor: "rgba(225, 117, 100, 0.1)",
												transform: "translateY(-2px)",
											},
											transition: "all 0.3s ease",
										}}>
										Email Me
									</Button>
								</motion.div>

								<motion.div
									whileHover={{ scale: 1.05 }}
									whileTap={{ scale: 0.95 }}>
									<Button
										variant="contained"
										size="large"
										startIcon={<DownloadIcon />}
										href="/NermeenNasim-SeniorSoftwareEngineer2025"
										download="NermeenNasimResume.pdf"
										sx={{
											backgroundColor: "#872341",
											color: "white",
											fontWeight: 600,
											px: 3,
											py: 1.5,
											borderRadius: 3,
											"&:hover": {
												backgroundColor: "#6b1d33",
												transform: "translateY(-2px)",
												boxShadow: "0 8px 25px rgba(135, 35, 65, 0.4)",
											},
											transition: "all 0.3s ease",
										}}>
										Download Resume
									</Button>
								</motion.div>
							</Box>
						</motion.div>
					</Box>
				</motion.div>
			</Container>
		</Box>
	);
};

export default Hero;
